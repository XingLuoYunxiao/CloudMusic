export enum EmitEventType {
  UPDATE_STATE
}

//自定义事件名称
export enum PublishEventType {
  CARD_PUBLISH = 'CARD_PUBLISH',
  APP_PUBLISH = 'APP_PUBLISH'
}

export const playStateKey = 'PlayStateKey'

//persistence键
export const SONG_KEY = 'PERSISTENCE_SONG_KEY'

